import { Logger } from '../../../SDK/logger';
import { Toast } from '../../../SDK/toast';

/**
 * 功能：提供 MemoryOS 运行时共享日志与提示服务。
 */
export const logger = new Logger('记忆引擎');
export const toast = new Toast('记忆引擎');

