# MemoryOS 提示词与整套记忆系统优化方案

> 适用项目：`ShionCox/SillyTavern-SS-Helper / MemoryOS`  
> 方案目标：把 MemoryOS 从“能总结、能注入”的功能型插件，优化成“稳定提炼、克制更新、可解释、可回放、可诊断”的长期记忆系统。  
> 建议优先级：先优化提示词策略与质量守卫，再优化 UI 诊断，最后再扩展向量/梦境/高级自动化。

---

## 1. 当前链路判断

当前 MemoryOS 已经具备比较完整的记忆主链：

```text
聊天消息
  ↓
SummaryService.captureSummaryFromChat()
  ↓
filterMemoryMessages()
  ↓
runSummaryOrchestrator()
  ↓
summary planner / mutation batch / validator / applier
  ↓
EntryRepository.applySummarySnapshot()
  ↓
PromptAssemblyService 检索与注入
```

当前提示词主要集中在：

```text
MemoryOS/src/memory-prompts/prompt-pack.md
```

其中已经包含：

```text
COLD_START_SYSTEM
COLD_START_CORE_SYSTEM
COLD_START_STATE_SYSTEM
SUMMARY_PLANNER_SYSTEM
SUMMARY_SYSTEM
TAKEOVER_BASELINE_SYSTEM
TAKEOVER_ACTIVE_SYSTEM
TAKEOVER_BATCH_SYSTEM
TAKEOVER_CONFLICT_RESOLUTION_SCHEMA
```

这说明 MemoryOS 已经不是简单摘要器，而是有：

```text
冷启动抽取
普通窗口总结
旧聊天接管
冲突解决
提示词注入
梦境整理
向量检索
```

但目前最大问题是：**提示词各段有能力，但缺少统一的“记忆提炼政策层”和“质量守卫闭环”。**

---

## 2. 优化总目标

本次优化不是让 AI “总结得更长”，而是让 AI：

1. 知道什么值得进入长期记忆；
2. 知道什么应该忽略；
3. 知道何时 ADD、UPDATE、MERGE、INVALIDATE、DELETE、NOOP；
4. 能区分角色、关系、任务、事件、世界状态、地点、组织、开放悬念；
5. 输出更稳定的 JSON；
6. 降低重复记忆、系统污染、过度推断；
7. 每条记忆都有依据、置信度、时间信息、reasonCodes；
8. 调试时能知道“为什么写入、为什么没写入、写入了哪里”。

---

## 3. 当前主要问题

### 3.1 Planner 太薄

当前 `SUMMARY_PLANNER_SYSTEM` 已经能判断是否值得更新，但规则偏简略：

```text
如窗口只是闲聊、重复确认或没有形成稳定事实，应返回 should_update=false。
```

这个方向对，但不够细。模型仍可能把：

```text
气氛描写
过渡对白
重复设定
系统提示
玩家调试语句
模型解释
```

误判成长期记忆。

### 3.2 Mutation 动作选择规则不够明确

当前 `SUMMARY_SYSTEM` 已经有：

```text
ADD 使用 newRecord；
UPDATE、MERGE、INVALIDATE 使用 patch；
DELETE 和 NOOP 不要输出 payload、patch、newRecord；
无法确认同一对象时优先 NOOP、UPDATE 或 MERGE，不要盲目 ADD。
```

这是正确方向，但还需要明确：

```text
什么时候 ADD
什么时候 UPDATE
什么时候 MERGE
什么时候 INVALIDATE
什么时候 DELETE
什么时候 NOOP
```

否则模型最容易出现的问题是：

```text
该 UPDATE 的时候 ADD
该 NOOP 的时候 ADD
该 INVALIDATE 的时候 DELETE
该 MERGE 的时候新增重复实体
```

### 3.3 缺少统一的“记忆价值判断”

目前提示词要求“长期稳定、可复用”，但没有系统化地告诉模型：

```text
长期记忆 ≠ 聊天摘要
长期记忆 ≠ 本轮剧情概述
长期记忆 ≠ 模型分析报告
长期记忆 = 后续检索会用到的稳定事实/变化/悬念
```

### 3.4 缺少 Source Trust 规则

SillyTavern/RP 场景里有很多内容不应该被写入正式记忆：

```text
system prompt
作者说明
meta 指令
格式要求
调试文本
模型自我解释
<think> 推理内容
工具日志
插件输出
```

如果不明确 source policy，模型会把这些污染进角色和世界设定。

### 3.5 缺少质量守卫输出

建议每条 action 增加：

```json
{
  "confidence": 0.82,
  "memoryValue": "high",
  "sourceEvidence": {
    "type": "story_dialogue",
    "brief": "艾琳主动共享撤离路线。"
  }
}
```

这样后续 UI 和测试才能判断为什么写入。

---

## 4. 推荐的系统架构优化

### 4.1 提示词分层

建议把提示词分成 6 层，不要所有规则都塞进 `SUMMARY_SYSTEM`。

```text
L1. Global Memory Policy
    统一规定什么是长期记忆、什么不该保存。

L2. Roleplay Memory Policy
    专门处理 SillyTavern/RP 场景：角色、关系、任务、事件、世界状态、悬念。

L3. Source Trust Policy
    判断哪些来源可以写入正式记忆，哪些只能作为候选或必须忽略。

L4. Planner Policy
    判断当前窗口是否值得更新，以及聚焦哪些类型。

L5. Mutation Action Policy
    判断 ADD / UPDATE / MERGE / INVALIDATE / DELETE / NOOP。

L6. Output Contract
    JSON schema、字段约束、语言风格、禁止词、reasonCodes。
```

### 4.2 推荐新增 prompt sections

建议在 `prompt-pack.md` 中新增以下 section：

```text
MEMORY_EXTRACTION_POLICY
ROLEPLAY_MEMORY_POLICY
SOURCE_TRUST_POLICY
MEMORY_ACTION_POLICY
MEMORY_QUALITY_POLICY
SUMMARY_PLANNER_SYSTEM_V2
SUMMARY_SYSTEM_V2
SUMMARY_QUALITY_GUARD_SYSTEM
```

这些 section 不一定都要单独调用，也可以由 prompt builder 拼接进现有 `SUMMARY_PLANNER_SYSTEM` / `SUMMARY_SYSTEM`。

---

## 5. 新增提示词：MEMORY_EXTRACTION_POLICY

建议加入到 `prompt-pack.md`：

```md
<!-- section: MEMORY_EXTRACTION_POLICY -->
你是 MemoryOS 的记忆提炼器，不是普通摘要器。

你的目标不是复述聊天内容，而是判断哪些信息值得进入长期记忆系统，并将它们提炼成稳定、可复用、可检索、可更新的结构化记忆。

核心原则：
1. 只保留长期有用的信息：
   - 稳定身份、人物设定、关系变化、任务进展、世界规则、地点组织、重要事件、未解悬念、用户/主角偏好。
   - 不保留普通寒暄、临时语气、重复确认、无后续价值的吐槽、纯格式说明、系统提示、工具日志。

2. 记忆必须是“可复用事实”，不是聊天摘要：
   - 错误示例：本轮对话主要讨论了艾琳和{{user}}的关系。
   - 正确示例：艾琳开始把{{user}}视为可以暂时托付后背的同伴，但仍保留戒心。

3. 优先提炼变化，而不是重复旧状态：
   - 如果只是重复已有关系、任务、地点或设定，应返回 NOOP。
   - 如果新信息让旧记忆更精确，应 UPDATE。
   - 如果新信息与旧记忆明显冲突，应 INVALIDATE、UPDATE 或 MERGE，不要盲目 ADD。
   - 如果无法确认是否同一对象，优先 NOOP 或低置信候选，不要新增重复实体。

4. 每条记忆都应尽量包含：
   - 主体：谁/什么对象发生变化；
   - 变化：关系、状态、任务、规则、地点、目标发生了什么；
   - 依据：来自本窗口中的明确行为、对话、结果或稳定叙事；
   - 时间：明确时间、相对时间、场景阶段或 sequence_fallback；
   - 置信度：明确事实高，合理推断中，不确定线索低。

5. 记忆语言应像故事设定集、角色小传、事件档案或悬念记录：
   - 自然、克制、叙事化；
   - 不要写成系统日志、分析报告或处理说明；
   - 禁止出现“本轮、本批次、当前系统、已识别、抽取结果、结构化处理”等系统腔表达。

6. 所有自然语言字段中，凡是指代主角/玩家/当前用户，一律使用 `{{user}}`。
   - 不要写真实用户名。
   - 不要写“你”“用户”“主角”“玩家”，统一替换为 `{{user}}`。

7. 对人物、组织、地点、任务、事件要分清类别：
   - 正式出场并参与行动/对话/关系推进的人物，才可进入 actor。
   - 群体词、身份称号、地点名、组织名、任务名不能误判成人物。
   - 只在分析、旁白、设定说明、未来构思里提到的人物，不要直接写入 actorCards。

8. 记忆提炼时必须克制：
   - 不要把一句话拆成过多碎片。
   - 优先形成少量高质量记忆。
   - 宁可少写，也不要写入不稳定、无依据、重复或过度推断的内容。
```

---

## 6. 新增提示词：ROLEPLAY_MEMORY_POLICY

```md
<!-- section: ROLEPLAY_MEMORY_POLICY -->
针对角色扮演、小说式互动、长篇剧情，请优先提炼以下类型的记忆：

1. 角色身份：
   - 名字、别名、身份、阵营、职业、能力、弱点、稳定性格；
   - 只有正式出场并产生剧情作用的人物才可写入。

2. 关系状态：
   - 信任、好感、敌意、依赖、承诺、误会、占有欲、合作程度；
   - 关系记忆要体现“变化”，不要只写“他们进行了交流”。

3. 任务与目标：
   - 当前目标、阶段、阻碍、完成条件、失败风险；
   - 任务推进要记录 from/to，而不是单纯写“任务继续”。

4. 世界与地点：
   - 世界规则、组织势力、地点危险、通行状态、资源状态；
   - 地点和组织不要误写成人物。

5. 事件档案：
   - 已发生并会影响后续剧情的事件；
   - 要包含参与者、地点、结果、影响。

6. 开放悬念：
   - 未解决问题、伏笔、约定、威胁、欠账、隐藏身份；
   - open_thread 应短而明确，方便后续召回。

禁止写入：
- 气氛描写本身，除非它导致关系或状态变化；
- 临时动作，除非它造成后果；
- 模型自言自语、推理痕迹、系统提示、格式说明；
- 对未来剧情的纯猜测；
- 玩家/用户的真实身份信息，除非是在故事设定内明确给出的角色信息。
```

---

## 7. 新增提示词：SOURCE_TRUST_POLICY

```md
<!-- section: SOURCE_TRUST_POLICY -->
你必须根据来源判断信息是否可写入正式记忆。

可作为正式抽取来源：
- story_dialogue：角色或{{user}}在故事内的对话；
- story_narrative：故事正文、行动描写、明确事件结果；
- user_story_instruction：用户明确要求写入故事设定、角色设定、世界设定；
- stable_worldbook：已知世界书或稳定设定文本。

只能作为弱候选来源：
- assistant_summary：AI 对前文的摘要；
- meta_discussion：关于剧情设计、提示词、插件设置的讨论；
- future_plan：用户或 AI 对未来剧情的设想；
- analysis_note：分析、解释、推断性内容。

禁止作为正式抽取来源：
- system prompt；
- developer instruction；
- tool artifact；
- error log；
- debug output；
- JSON schema 示例；
- 代码；
- <think>、reasoning、隐藏推理；
- 模型自我说明；
- 插件内部状态；
- 用户对插件/提示词/格式的调试要求。

如果同一事实只出现在弱候选来源中：
- 不要直接 ADD；
- 可输出低置信 open_thread 或 NOOP；
- 除非后续 story_dialogue / story_narrative 明确确认。
```

---

## 8. 新增提示词：MEMORY_ACTION_POLICY

```md
<!-- section: MEMORY_ACTION_POLICY -->
在生成 actions 时，必须根据新信息与已有记忆的关系选择动作：

ADD：
- 当前窗口出现新的稳定对象、事件、任务、关系或世界状态；
- 现有记忆中没有等价内容；
- 新信息未来可能被检索并影响角色行为或剧情判断。

UPDATE：
- 当前窗口补充了已有对象的新状态、新细节或新阶段；
- 新信息与旧信息是同一对象、同一关系、同一任务或同一事件的延续；
- 更新后应保留旧记忆中仍然有效的部分，不要覆盖掉重要历史。

MERGE：
- 当前窗口或已有记忆中存在多个疑似同一对象；
- 它们名称、别名、地点、参与者、目标高度重合；
- 合并后应形成一个更完整、更稳定的记忆。

INVALIDATE：
- 当前窗口明确推翻、废止、纠正了旧记忆；
- 旧记忆不应再被直接召回，但可以作为历史痕迹保留；
- 适用于关系破裂、任务取消、地点失效、规则改变等情况。

DELETE：
- 仅用于明显错误、无意义、系统污染、重复垃圾记忆；
- 不要因为剧情变化就 DELETE，剧情变化优先 INVALIDATE 或 UPDATE。

NOOP：
- 信息已被现有记忆完整覆盖；
- 当前窗口只是重复、闲聊、语气变化或无长期价值；
- 无法确认事实稳定性或来源可靠时，优先 NOOP。

质量要求：
- 每条 action 必须有明确 reasonCodes。
- 每条新记忆应尽量短，但必须完整表达事实。
- 不要输出“可能、大概、似乎”这类模糊词，除非 confidence 较低且作为线索保存。
- 不要为了凑数量输出 actions。
```

---

## 9. 改造 SUMMARY_PLANNER_SYSTEM

建议替换为：

```md
<!-- section: SUMMARY_PLANNER_SYSTEM -->
你正在执行 MemoryOS summary planner。

你的职责不是总结聊天，而是判断当前窗口是否产生了“值得进入长期记忆系统的稳定变化”，并给出后续 mutation 的聚焦方向。

请按以下步骤判断，但最终只输出 JSON：

第一步：判断窗口是否有长期记忆价值。

应更新的情况：
- 出现新的稳定人物、组织、地点、任务、事件、世界规则；
- 已有人物关系发生明显变化；
- 任务状态发生推进、失败、转向、完成；
- 世界状态、地点状态、阵营状态发生变化；
- 出现未来仍需要追踪的悬念、承诺、约定、目标；
- 出现会影响后续剧情或角色行为的事实。

不应更新的情况：
- 普通闲聊、气氛描写、重复确认；
- 没有产生新事实的过渡对话；
- 单纯复述旧设定；
- 只是用户要求格式、调试、系统说明；
- 模型自我解释、工具日志、无故事意义的元信息。

第二步：判断变化类型。

从以下类型中选择 focus_types：
- identity：人物身份、性格、称呼、别名、阵营发生稳定变化；
- relationship：关系、信任、好感、敌意、承诺、依赖发生变化；
- task：任务目标、状态、阻碍、完成条件发生变化；
- event：发生了值得追踪的事件；
- world_global_state：世界规则、局势、组织状态、城市状态变化；
- location：地点状态、可达性、危险程度、归属变化；
- organization：组织、阵营、派系相关稳定信息；
- open_thread：未解决悬念、伏笔、待办目标；
- no_update：没有值得写入的内容。

第三步：判断是否可能重复。

如果当前窗口只是重复已有记忆，应返回 should_update=false。
如果当前窗口补充了旧记忆缺失的关键细节，应返回 should_update=true，并在 reasons 中说明“应更新而非新增”。

输出要求：
- 所有自然语言字段中，凡是指代主角/玩家/当前用户，一律使用 `{{user}}`。
- 不要输出 Markdown。
- 只输出 JSON。
```

### 建议升级 schema

```json
{
  "type": "object",
  "required": [
    "should_update",
    "focus_types",
    "entities",
    "topics",
    "reasons",
    "memory_value",
    "suggested_operation_bias",
    "skip_reason"
  ],
  "properties": {
    "should_update": { "type": "boolean" },
    "focus_types": { "type": "array", "items": { "type": "string" } },
    "entities": { "type": "array", "items": { "type": "string" } },
    "topics": { "type": "array", "items": { "type": "string" } },
    "reasons": { "type": "array", "items": { "type": "string" } },
    "memory_value": { "type": "string", "enum": ["none", "low", "medium", "high"] },
    "suggested_operation_bias": {
      "type": "array",
      "items": { "type": "string", "enum": ["ADD", "UPDATE", "MERGE", "INVALIDATE", "DELETE", "NOOP"] }
    },
    "skip_reason": { "type": "string" }
  }
}
```

### 输出样例

```json
{
  "should_update": true,
  "focus_types": ["relationship", "task"],
  "entities": ["user", "actor_erin", "entity:task:escort_messenger"],
  "topics": ["关系推进", "撤离任务"],
  "reasons": [
    "艾琳对{{user}}的信任出现明确提升，撤离任务也从筹划进入执行。"
  ],
  "memory_value": "high",
  "suggested_operation_bias": ["UPDATE", "MERGE"],
  "skip_reason": ""
}
```

无更新样例：

```json
{
  "should_update": false,
  "focus_types": ["no_update"],
  "entities": [],
  "topics": [],
  "reasons": [],
  "memory_value": "none",
  "suggested_operation_bias": ["NOOP"],
  "skip_reason": "当前窗口只是重复确认既有任务，没有形成新的稳定事实。"
}
```

---

## 10. 改造 SUMMARY_SYSTEM

建议替换为：

```md
<!-- section: SUMMARY_SYSTEM -->
你正在执行 MemoryOS summary mutation。

你不是普通摘要器。你的任务是根据当前窗口、已有记忆和 planner 结果，输出真正需要落库的稀疏 mutation。

必须遵守：
- ADD 使用 newRecord；
- UPDATE、MERGE、INVALIDATE 使用 patch；
- DELETE 和 NOOP 不要输出 payload、patch、newRecord；
- compareKey 采用 ck:v2 协议；
- entityKey 是内部稳定键；
- matchKeys 只做模糊候选；
- 无法确认同一对象时优先 NOOP、UPDATE 或 MERGE，不要盲目 ADD；
- 所有自然语言字段中，凡是指代主角/玩家/当前用户，一律使用 `{{user}}`；
- 只输出 JSON。

记忆价值原则：
- 输出长期可复用的事实、状态变化、关系变化、任务进展、世界状态、开放悬念；
- 不要输出聊天过程说明；
- 不要输出系统分析；
- 不要输出重复旧记忆；
- 不要为了凑数量输出 actions；
- 宁可少写，也不要写脏记忆。

动作选择：
- 新对象或新事实：ADD；
- 旧对象补充新状态：UPDATE；
- 多个疑似同一对象：MERGE；
- 旧状态被推翻或过期：INVALIDATE；
- 明显错误或污染：DELETE；
- 没有长期价值或已被覆盖：NOOP。

时间规则：
- 每条 action 必须尽量包含 timeContext 信息。
- 如果对话中存在明确时间表达，如“次日清晨”“三天后”，mode 设为 story_explicit，并在 storyTime 中记录原文。
- 如果可推断时间进展但无明确表达，如场景切换、睡眠暗示，mode 设为 story_inferred。
- 禁止凭空捏造精确日期，不确定时使用 sequence_fallback。
- confidence 范围 0~1，明确时间通常 >= 0.8，推断时间 0.5~0.8，兜底 <= 0.4。

证据规则：
- 每条 ADD / UPDATE / MERGE / INVALIDATE 都必须包含 sourceEvidence。
- sourceEvidence.brief 只能概括故事内依据，不要写系统分析。
- 如果没有可靠依据，使用 NOOP。
```

### 建议升级 schema

```json
{
  "type": "object",
  "required": ["schemaVersion", "window", "actions", "diagnostics"],
  "properties": {
    "schemaVersion": { "type": "string" },
    "window": {
      "type": "object",
      "required": ["fromTurn", "toTurn"]
    },
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["action", "targetKind", "reasonCodes"],
        "properties": {
          "action": {
            "type": "string",
            "enum": ["ADD", "UPDATE", "MERGE", "INVALIDATE", "DELETE", "NOOP"]
          },
          "targetKind": { "type": "string" },
          "entityKey": { "type": "string" },
          "compareKey": { "type": "string" },
          "matchKeys": { "type": "array", "items": { "type": "string" } },
          "schemaVersion": { "type": "string" },
          "confidence": { "type": "number" },
          "memoryValue": {
            "type": "string",
            "enum": ["low", "medium", "high"]
          },
          "sourceEvidence": {
            "type": "object",
            "properties": {
              "type": { "type": "string" },
              "brief": { "type": "string" },
              "turnRefs": { "type": "array", "items": { "type": "number" } }
            }
          },
          "timeContext": {
            "type": "object",
            "properties": {
              "mode": {
                "type": "string",
                "enum": ["story_explicit", "story_inferred", "sequence_fallback"]
              },
              "storyTime": { "type": "string" },
              "confidence": { "type": "number" }
            }
          },
          "patch": { "type": "object", "additionalProperties": true },
          "newRecord": { "type": "object", "additionalProperties": true },
          "reasonCodes": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "diagnostics": {
      "type": "object",
      "properties": {
        "skippedCount": { "type": "number" },
        "noopReasons": { "type": "array", "items": { "type": "string" } },
        "possibleDuplicates": { "type": "array", "items": { "type": "string" } },
        "sourceWarnings": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
```

### 输出样例

```json
{
  "schemaVersion": "1.1.0",
  "window": {
    "fromTurn": 101,
    "toTurn": 120
  },
  "actions": [
    {
      "action": "UPDATE",
      "targetKind": "relationship",
      "entityKey": "relationship:user:actor_erin:朋友",
      "compareKey": "ck:v2:relationship:user:actor_erin:朋友",
      "matchKeys": ["mk:relationship:user:actor_erin"],
      "schemaVersion": "v2",
      "confidence": 0.84,
      "memoryValue": "high",
      "sourceEvidence": {
        "type": "story_dialogue",
        "brief": "艾琳主动把撤离路线告诉{{user}}，并允许{{user}}同行。",
        "turnRefs": [108, 109]
      },
      "timeContext": {
        "mode": "story_inferred",
        "storyTime": "撤离前夜",
        "confidence": 0.68
      },
      "patch": {
        "summary": "艾琳对{{user}}的戒心仍在，但已经愿意在险局中共享关键情报。",
        "state": "谨慎合作",
        "trust": 0.56,
        "tension": 0.24
      },
      "reasonCodes": [
        "relationship_progressed",
        "dialogue_evidence_present",
        "update_existing_preferred"
      ]
    }
  ],
  "diagnostics": {
    "skippedCount": 2,
    "noopReasons": ["重复的撤离目标没有再次写入。"],
    "possibleDuplicates": [],
    "sourceWarnings": []
  }
}
```

---

## 11. 新增质量守卫：SUMMARY_QUALITY_GUARD_SYSTEM

建议新增一个轻量二次检查，不一定每次都调用 LLM。可以先做代码规则校验，必要时再让 LLM 修复。

```md
<!-- section: SUMMARY_QUALITY_GUARD_SYSTEM -->
你正在执行 MemoryOS summary quality guard。

你的任务是检查 mutation document 是否存在以下问题，并输出修正后的 JSON：

必须修复的问题：
1. JSON 不合法；
2. action 缺少必要字段；
3. ADD 缺少 newRecord；
4. UPDATE / MERGE / INVALIDATE 缺少 patch；
5. DELETE / NOOP 包含 patch 或 newRecord；
6. 自然语言字段中出现“用户”“主角”“玩家”“你”，应改为 `{{user}}`；
7. 出现系统腔表达，如“本轮”“当前系统”“抽取结果”“结构化处理”；
8. sourceEvidence 来自禁止来源；
9. confidence 超出 0~1；
10. compareKey 不是 ck:v2 协议；
11. 明显重复的 ADD 应改为 UPDATE / MERGE / NOOP；
12. 无依据的高置信推断应降置信或改为 NOOP。

不要新增没有依据的记忆。
不要扩大原文含义。
只输出修正后的 JSON。
```

---

## 12. 代码层建议

### 12.1 给 SummaryOrchestrator 增加策略拼接

建议在 prompt 构建处支持：

```ts
const policyPrompt = [
  readPromptSection('MEMORY_EXTRACTION_POLICY'),
  readPromptSection('ROLEPLAY_MEMORY_POLICY'),
  readPromptSection('SOURCE_TRUST_POLICY'),
  readPromptSection('MEMORY_ACTION_POLICY'),
].join('\n\n');
```

然后：

```text
SUMMARY_PLANNER_SYSTEM = MEMORY_EXTRACTION_POLICY + ROLEPLAY_MEMORY_POLICY + SOURCE_TRUST_POLICY + SUMMARY_PLANNER_SYSTEM
SUMMARY_SYSTEM = MEMORY_EXTRACTION_POLICY + ROLEPLAY_MEMORY_POLICY + SOURCE_TRUST_POLICY + MEMORY_ACTION_POLICY + SUMMARY_SYSTEM
```

这样不要把每段 prompt 写得太臃肿，也方便以后开关不同策略。

### 12.2 给 mutation validator 增加硬规则

建议在 `mutation-validator` 或 `mutation-patch-utils` 附近增加这些检查：

```text
action 字段合法性
payload/patch/newRecord 与 action 匹配
compareKey 协议检查
entityKey 空值检查
confidence 范围检查
reasonCodes 非空检查
{{user}} 替换检查
禁止词检查
sourceEvidence 检查
重复 ADD 检查
```

伪代码：

```ts
function validateActionContract(action: SummaryMutationAction): ValidationIssue[] {
  const issues: ValidationIssue[] = [];

  if (!['ADD', 'UPDATE', 'MERGE', 'INVALIDATE', 'DELETE', 'NOOP'].includes(action.action)) {
    issues.push({ level: 'error', code: 'invalid_action' });
  }

  if (action.action === 'ADD' && !action.newRecord) {
    issues.push({ level: 'error', code: 'add_missing_newRecord' });
  }

  if (['UPDATE', 'MERGE', 'INVALIDATE'].includes(action.action) && !action.patch) {
    issues.push({ level: 'error', code: 'patch_missing' });
  }

  if (['DELETE', 'NOOP'].includes(action.action) && (action.patch || action.newRecord)) {
    issues.push({ level: 'error', code: 'noop_delete_should_not_have_payload' });
  }

  if (action.compareKey && !String(action.compareKey).startsWith('ck:v2:')) {
    issues.push({ level: 'warn', code: 'comparekey_not_v2' });
  }

  if (typeof action.confidence === 'number' && (action.confidence < 0 || action.confidence > 1)) {
    issues.push({ level: 'error', code: 'confidence_out_of_range' });
  }

  return issues;
}
```

### 12.3 增加 source pollution filter

建议做一个字符串扫描：

```ts
const SYSTEM_TONE_PATTERNS = [
  /本轮/g,
  /当前系统/g,
  /抽取结果/g,
  /结构化处理/g,
  /已识别/g,
  /该批次/g,
  /输出内容/g,
  /处理结果/g,
];

const USER_ALIAS_PATTERNS = [
  /用户/g,
  /主角/g,
  /玩家/g,
  /你/g,
];
```

对自然语言字段进行清洗或拒绝。

注意：`你` 在角色对白里可能是正常称呼，不建议一刀切替换。更安全做法是只检查 memory fields：

```text
summary
state
description
goal
reason
openThreads
identityFacts
originFacts
traits
```

而不是扫描全部原文。

---

## 13. 数据结构建议

### 13.1 SummaryMutationAction 建议扩展字段

建议扩展：

```ts
type MemoryValue = 'low' | 'medium' | 'high';
type SourceEvidenceType =
  | 'story_dialogue'
  | 'story_narrative'
  | 'user_story_instruction'
  | 'stable_worldbook'
  | 'assistant_summary'
  | 'meta_discussion'
  | 'unknown';

interface SourceEvidence {
  type: SourceEvidenceType;
  brief: string;
  turnRefs?: number[];
}

interface SummaryMutationAction {
  action: 'ADD' | 'UPDATE' | 'MERGE' | 'INVALIDATE' | 'DELETE' | 'NOOP';
  targetKind: string;
  entityKey?: string;
  compareKey?: string;
  matchKeys?: string[];
  schemaVersion?: string;

  confidence?: number;
  memoryValue?: MemoryValue;
  sourceEvidence?: SourceEvidence;
  timeContext?: {
    mode: 'story_explicit' | 'story_inferred' | 'sequence_fallback';
    storyTime?: string;
    confidence: number;
  };

  patch?: Record<string, unknown>;
  newRecord?: Record<string, unknown>;
  reasonCodes: string[];
}
```

### 13.2 SummarySnapshot 建议记录诊断

```ts
interface SummarySnapshotDiagnostics {
  planner: {
    shouldUpdate: boolean;
    memoryValue: 'none' | 'low' | 'medium' | 'high';
    focusTypes: string[];
    skipReason?: string;
  };
  mutation: {
    actionCount: number;
    addCount: number;
    updateCount: number;
    mergeCount: number;
    invalidateCount: number;
    deleteCount: number;
    noopCount: number;
  };
  qualityGuard: {
    issueCount: number;
    fixedCount: number;
    droppedCount: number;
    warnings: string[];
  };
}
```

---

## 14. UI 诊断优化

建议在 MemoryOS 工作台增加“本轮记忆提炼诊断”面板。

### 14.1 总览卡

```text
本轮窗口：101-120 楼
Planner：should_update=true
记忆价值：high
聚焦类型：relationship / task
Mutation：1 UPDATE, 0 ADD, 0 DELETE
质量守卫：0 error, 1 warning
写入结果：成功
```

### 14.2 Action 列表

每条 action 展示：

```text
动作：UPDATE
类型：relationship
对象：艾琳 ↔ {{user}}
置信度：0.84
记忆价值：high
依据：艾琳主动把撤离路线告诉{{user}}，并允许{{user}}同行。
reasonCodes:
- relationship_progressed
- dialogue_evidence_present
- update_existing_preferred
```

### 14.3 跳转能力

建议支持：

```text
查看原始窗口
查看 prompt
查看 LLM 原始返回
查看修复后的 JSON
查看写入前后 diff
复制测试包
```

这会极大提升调试效率。

---

## 15. 测试体系优化

### 15.1 增加 prompt fixture

建议新增目录：

```text
MemoryOS/test/fixtures/memory-summary/
```

放这些测试：

```text
01_no_update_chitchat.json
02_relationship_update.json
03_task_progress.json
04_duplicate_should_noop.json
05_meta_pollution_should_ignore.json
06_location_not_actor.json
07_conflict_should_invalidate.json
08_same_entity_should_merge.json
09_open_thread.json
10_time_context_explicit.json
```

### 15.2 每个 fixture 结构

```json
{
  "name": "relationship_update",
  "messages": [
    { "role": "user", "content": "..." },
    { "role": "assistant", "content": "..." }
  ],
  "existingEntries": [],
  "expected": {
    "plannerShouldUpdate": true,
    "mustHaveActions": ["UPDATE"],
    "mustNotHaveActions": ["ADD"],
    "requiredReasonCodes": ["relationship_progressed"],
    "forbiddenPhrases": ["本轮", "用户", "主角", "结构化"]
  }
}
```

### 15.3 断言重点

```text
JSON 可解析
schemaVersion 存在
action 合法
ADD/UPDATE/DELETE 结构正确
{{user}} 替换正确
没有系统腔
没有把地点/组织误判成角色
重复信息不会 ADD
低质量窗口 should_update=false
```

---

## 16. 分阶段落地计划

### 阶段一：提示词增强，低风险

改动文件：

```text
MemoryOS/src/memory-prompts/prompt-pack.md
```

新增：

```text
MEMORY_EXTRACTION_POLICY
ROLEPLAY_MEMORY_POLICY
SOURCE_TRUST_POLICY
MEMORY_ACTION_POLICY
```

替换：

```text
SUMMARY_PLANNER_SYSTEM
SUMMARY_SYSTEM
```

目标：

```text
减少垃圾记忆
减少重复 ADD
提升 UPDATE / MERGE 准确率
提升角色/组织/地点分类准确率
```

### 阶段二：Validator 增强，中风险

改动区域：

```text
MemoryOS/src/memory-summary/mutation-validator.ts
MemoryOS/src/memory-summary/mutation-patch-utils.ts
```

增加：

```text
action contract 校验
confidence 校验
compareKey 校验
sourceEvidence 校验
禁止词校验
{{user}} 校验
```

目标：

```text
模型输出轻微跑偏时，系统能挡住。
```

### 阶段三：诊断面板，高收益

改动区域：

```text
MemoryOS/src/ui/unifiedMemoryWorkbench*
MemoryOS/src/ui/chatStrategyPanel*
MemoryOS/src/sdk/editor-facade*
```

增加：

```text
本轮提炼诊断
Action diff
Quality guard warnings
Prompt replay bundle
```

目标：

```text
出现“为什么没记住/为什么乱记”时能快速定位。
```

### 阶段四：测试包与回放，高稳定性

新增：

```text
MemoryOS/test/fixtures/memory-summary/
MemoryOS/test/summary-prompt-policy.spec.ts
MemoryOS/test/summary-quality-guard.spec.ts
```

目标：

```text
以后改 prompt 不会把记忆主链改坏。
```

---

## 17. 推荐优先级

### P0：马上做

```text
1. 加 MEMORY_EXTRACTION_POLICY
2. 加 ROLEPLAY_MEMORY_POLICY
3. 加 MEMORY_ACTION_POLICY
4. 替换 SUMMARY_PLANNER_SYSTEM
5. 替换 SUMMARY_SYSTEM
```

### P1：随后做

```text
1. mutation validator 增加 action contract 检查
2. summary 输出增加 confidence / memoryValue / sourceEvidence
3. 增加 no_update / duplicate / meta pollution 测试
```

### P2：体验增强

```text
1. 工作台增加提炼诊断面板
2. 每条记忆显示 sourceEvidence 和 reasonCodes
3. 支持一键导出 prompt replay bundle
```

### P3：高级能力

```text
1. 自动策略：轻量/均衡/深度/省钱模式
2. 向量检索状态自动切换 lexical/hybrid
3. 梦境系统与 summary mutation 共享 MEMORY_ACTION_POLICY
4. 旧聊天接管加质量守卫
```

---

## 18. 最终建议

MemoryOS 现在的功能面已经够宽，不建议继续先加新功能。

更应该先把系统从：

```text
能总结
```

升级成：

```text
知道什么该记
知道什么不该记
知道怎么更新旧记忆
知道怎么避免污染
能解释每条记忆为什么写入
能回放和测试每次提炼结果
```

最关键的落点是：

```text
prompt-pack.md 增加策略层
summary mutation 增加 evidence / confidence / memoryValue
validator 增加硬规则
UI 增加诊断面板
测试增加 fixture 回放
```

做完这些，MemoryOS 的可靠性会明显上一个台阶，后续再做梦境、向量、自动调度才更稳。
